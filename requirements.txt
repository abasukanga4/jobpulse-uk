# Runtime dependencies for the deployed Streamlit dashboard
# (Hugging Face Spaces / Streamlit Community Cloud).
# The final `.` installs the first-party `jobpulse` package and its core deps
# (duckdb, pandas, pydantic, structlog, ...) from pyproject.toml; the lines
# above add the model + dashboard runtime libraries.
streamlit>=1.39
plotly>=5.24
scikit-learn>=1.5
xgboost>=2.1
joblib>=1.3
.
