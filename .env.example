# Copy to .env and fill in. Never commit .env.
# Adzuna — free tier at https://developer.adzuna.com (250 calls/month)
ADZUNA_APP_ID=
ADZUNA_APP_KEY=

# Anthropic — for LLM skill extraction. Optional: regex fallback is used if unset.
ANTHROPIC_API_KEY=

# Storage
JOBPULSE_DATA_DIR=./data
JOBPULSE_DB_PATH=./data/jobpulse.duckdb
